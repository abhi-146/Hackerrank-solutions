
	public static int getHeight(Node root){
      if(root==null)
      return -1;
      else 
      {
         int left=getHeight(root.left);
         int right=getHeight(root.right);
         if(left>right)
         return (left+1);
         else 
         return (right+1);
      }
    }
